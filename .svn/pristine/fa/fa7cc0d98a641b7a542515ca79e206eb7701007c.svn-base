/**
* 第三方授权认证
*/
var mongoose = require('mongoose');

exports.AuthorizeSchema = new mongoose.Schema({
   
    name: String,
    appKey: String,
    appSecret:String,
    desc:String,
    createUsername:String,
    DateTime:{ type: Date, default: Date.now }
});